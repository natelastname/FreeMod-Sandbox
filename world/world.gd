class_name FreeModWorld

extends Spatial

# The idea behind this script is to handle the stuff that is lower-level than the
# gamemode code. For example:
# - If multiplayer is added, this script could control sync/netcode.
# - The pause menu, the debug menu, stuff that all gamemodes should share.

func _ready():
	pass
	
func _process(delta):
	pass
